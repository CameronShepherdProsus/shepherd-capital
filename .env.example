# Copy this file to .env and fill in values you want to use.
# The system works without any API keys — they only enable enhanced LLM extraction.

# Optional: enables LLM-assisted recommendation extraction
ANTHROPIC_API_KEY=
OPENAI_API_KEY=

# Optional: override database path
# DATABASE_URL=sqlite:///data/rebound_tracker.sqlite
